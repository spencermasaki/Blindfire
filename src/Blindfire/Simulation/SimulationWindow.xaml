<Window x:Class="Blindfire.Simulation.SimulationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Blindfire - Sensitivity Simulator"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Background="#FF0B0D12"
        Foreground="{StaticResource TextPrimaryBrush}">
    <Grid x:Name="RootGrid">

        <Viewport3D x:Name="MainViewport">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="MainCamera" Position="0,1.7,0" LookDirection="0,0,-1" UpDirection="0,1,0"
                                    FieldOfView="70" NearPlaneDistance="0.05" FarPlaneDistance="200"/>
            </Viewport3D.Camera>
        </Viewport3D>

        <Canvas x:Name="HudCanvas" IsHitTestVisible="False">
            <Path x:Name="ScopeVignette" Fill="Black" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Line x:Name="CrosshairTop" Stroke="White" StrokeThickness="2"/>
            <Line x:Name="CrosshairBottom" Stroke="White" StrokeThickness="2"/>
            <Line x:Name="CrosshairLeft" Stroke="White" StrokeThickness="2"/>
            <Line x:Name="CrosshairRight" Stroke="White" StrokeThickness="2"/>
            <Ellipse x:Name="AdsDot" Width="6" Height="6" Fill="White" Visibility="Collapsed"/>
        </Canvas>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
            <TextBlock x:Name="StatsText" FontSize="16" HorizontalAlignment="Center"/>
            <TextBlock Text="Tab: settings &#183; Esc: exit &#183; Hold right mouse: ADS" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <Border x:Name="SettingsOverlay" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource SurfaceBorderBrush}" BorderThickness="1"
                CornerRadius="12" Padding="24" MinWidth="420">
            <StackPanel>
                <TextBlock Text="Sensitivity Simulator Settings" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <TextBlock Text="Scope" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <Button x:Name="Scope2xButton" Content="2x" Tag="2" Width="56" Margin="0,0,8,0" Click="OnScopeButtonClicked"/>
                    <Button x:Name="Scope3xButton" Content="3x" Tag="3" Width="56" Margin="0,0,8,0" Click="OnScopeButtonClicked"/>
                    <Button x:Name="Scope4xButton" Content="4x" Tag="4" Width="56" Margin="0,0,8,0" Click="OnScopeButtonClicked"/>
                    <Button x:Name="Scope6xButton" Content="6x" Tag="6" Width="56" Margin="0,0,8,0" Click="OnScopeButtonClicked"/>
                    <Button x:Name="Scope10xButton" Content="10x" Tag="10" Width="56" Click="OnScopeButtonClicked"/>
                </StackPanel>

                <CheckBox x:Name="MovingTargetsCheckBox" Content="Moving targets" IsChecked="True" Margin="0,0,0,16" Click="OnMovingTargetsToggled"/>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Sensitivity" VerticalAlignment="Center"/>
                    <TextBox x:Name="SensitivityBox" Grid.Column="1"/>
                </Grid>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="ADS multiplier" VerticalAlignment="Center"/>
                    <TextBox x:Name="AdsMultiplierBox" Grid.Column="1"/>
                </Grid>
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Horizontal FOV" VerticalAlignment="Center"/>
                    <TextBox x:Name="FovBox" Grid.Column="1"/>
                </Grid>

                <TextBlock x:Name="SettingsValidationText" Foreground="#FF8080" TextWrapping="Wrap" Margin="0,8,0,8"/>

                <Button x:Name="ApplySettingsButton" Content="Apply" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center" Margin="0,4,0,12" Click="OnApplySettingsClicked"/>
                <TextBlock Text="Tab to resume" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

    </Grid>
</Window>
